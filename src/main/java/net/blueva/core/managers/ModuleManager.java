package net.blueva.core.managers;

import net.blueva.core.Main;
import net.blueva.core.utils.MessagesUtil;
import org.bukkit.Bukkit;
import org.bukkit.ChatColor;
import org.bukkit.scheduler.BukkitRunnable;

public class ModuleManager {

    public static void loadAllModules(Main main) {
        /*loadModule(main, "auth");
        loadModule(main, "bossbar");
        loadModule(main, "chat");
        loadModule(main, "commands");
        loadModule(main, "economy");
        loadModule(main, "holograms");
        loadModule(main, "kits");
        loadModule(main, "permissions");
        loadModule(main, "scoreboards");
        loadModule(main, "tablist");
        loadModule(main, "warps");
        loadModule(main, "welcome");
        loadModule(main, "worlds");*/
    }

}
